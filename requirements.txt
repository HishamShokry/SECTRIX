Django>=5.0,<5.2
psycopg[binary]>=3.1,<4.0  # PostgreSQL driver (production). Remove for SQLite-only dev.
Pillow>=10.0,<12.0          # ImageField support for CaseStudy.cover_image
gunicorn>=21.2,<24.0        # Production WSGI server
whitenoise>=6.6,<7.0        # Static asset serving in production
